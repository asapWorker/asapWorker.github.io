import { VoicePlayerSettings } from '../typings';
export declare type EventsType = {
    play: (trackId: string) => void;
    end: (trackId: string) => void;
};
export declare const createVoicePlayer: ({ startVoiceDelay, sampleRate, numberOfChannels, }?: VoicePlayerSettings) => {
    append: (data: Uint8Array, trackId: string, last?: boolean) => void;
    active: boolean;
    on: <K extends "play" | "end">(event: K, cb: EventsType[K]) => () => void;
    stop: () => void;
};
//# sourceMappingURL=index.d.ts.map